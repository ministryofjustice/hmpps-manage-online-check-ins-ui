---
# Per environment values which override defaults in hmpps-manage-online-check-ins-ui/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: manage-online-check-ins-ui-dev.hmpps.service.justice.gov.uk

  env:
    INGRESS_URL: "https://manage-online-check-ins-ui-dev.hmpps.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in-dev.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api-dev.prison.service.justice.gov.uk"
    ESUPERVISION_API_URL: 'https://esupervision-api-dev.hmpps.service.justice.gov.uk'
    ENVIRONMENT_NAME: DEV
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: warning
