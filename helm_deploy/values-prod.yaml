---
# Per environment values which override defaults in hmpps-manage-online-check-ins-ui/values.yaml

generic-service:
  ingress:
    host: manage-online-check-ins.hmpps.service.justice.gov.uk

  env:
    ENVIRONMENT: 'prod'
    INGRESS_URL: "https://manage-online-check-ins.hmpps.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api.prison.service.justice.gov.uk"
    ESUPERVISION_API_URL: 'https://esupervision-api.hmpps.service.justice.gov.uk'
    AUDIT_ENABLED: "false"
    COMPONENT_API_URL: "https://probation-frontend-components.hmpps.service.justice.gov.uk"
    ENVIRONMENT_NAME: "PRODUCTION"
    MANAGE_PEOPLE_ON_PROBATION_LINK: "https://manage-people-on-probation.hmpps.service.justice.gov.uk"

generic-prometheus-alerts:
  alertSeverity: warning
